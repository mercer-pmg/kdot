% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refresh_reporting_cache.R
\name{wait_for_cache_refresh}
\alias{wait_for_cache_refresh}
\title{Wait for Cache Refresh to Complete}
\usage{
wait_for_cache_refresh(
  token = get_token(),
  baseline_db,
  baseline_bm,
  interval_min = 3L,
  max_wait_min = 120L
)
}
\arguments{
\item{token}{API authentication token (default: get_token())}

\item{baseline_db}{Baseline database performance table (from
\code{build_database_performance_table})}

\item{baseline_bm}{Baseline benchmark performance table (from
\code{build_benchmark_performance_table})}

\item{interval_min}{Minutes between polls (default: 3)}

\item{max_wait_min}{Maximum minutes to wait before timing out (default: 120)}
}
\value{
TRUE if data changed (cache updated), FALSE if timeout
}
\description{
Polls the Homepage/Performance endpoint every \code{interval_min} minutes until
the returned data differs from the baseline, or until \code{max_wait_min}
elapses. Call this after \code{\link{refresh_reporting_cache}}; capture the
baseline (from \code{\link{fetch_homepage_performance}} and
\code{\link{build_database_performance_table}} / \code{\link{build_benchmark_performance_table}})
before posting the refresh request.
}
