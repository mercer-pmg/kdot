% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_trading_model_classification.R
\name{update_trading_model_classification}
\alias{update_trading_model_classification}
\alias{batch_update_trading_model_classifications}
\title{Update Orion Trading Model \code{assetClassId} and/or \code{riskCategoryId}}
\usage{
update_trading_model_classification(
  model_id,
  token,
  asset_class_id = NULL,
  risk_category_id = NULL
)

batch_update_trading_model_classifications(df, token)
}
\arguments{
\item{model_id}{Orion trading model id (integer-coercible).}

\item{token}{Orion API bearer token.}

\item{asset_class_id}{New \code{assetClassId}, or \code{NULL} / \code{NA} to leave unchanged.}

\item{risk_category_id}{New \code{riskCategoryId}, or \code{NULL} / \code{NA} to leave unchanged.}

\item{df}{A data frame with column \code{modelId} (required). Optional columns
\code{riskCategoryId} and \code{assetClassId}. Rows may use \code{NA} or blanks to skip
a field. Also accepts snake_case names \code{model_id}, \code{risk_category_id},
\code{asset_class_id} when the camelCase name is absent.}
}
\value{
A list with \code{success} (logical), \code{model_id}, \code{message} (character),
and \code{model_name} (character, may be \code{NA}).

A data frame with columns \code{success}, \code{model_id}, \code{message}, \code{model_name}.
}
\description{
Fetches the model via GET \verb{/api/v1/Trading/Models/\{id\}}, applies ID updates,
then PUTs the full JSON payload. Uses \code{orion_request()} for the PUT so empty
or non-JSON success bodies (e.g. 204) do not fail parsing.
}
\examples{
\dontrun{
token <- get_token()
update_trading_model_classification(
  12345L,
  token,
  risk_category_id = 99L
)
}
}
